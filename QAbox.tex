\documentclass{jlreq}
\usepackage{graphicx,tcolorbox,diffcoeff}
\usepackage{amsmath,amsthm}
\usepackage{unicode-math}
\setmathfont{NewComputerModernMath}
\usepackage[thicklines]{cancel}
\usepackage[hidelinks]{hyperref}
%使いたいパッケージは競合してエラー置きない限り自由に追加していいよ
\tcbuselibrary{breakable, skins}
\newcommand{\qsep}{%
  \tcblower
  \medskip
}

\newtcolorbox{hako}[1]{colframe=black,colback=white,coltitle=black,colbacktitle=white,boxrule=0.8pt,arc=0mm,title={\textbf{#1}},}

\newtcolorbox{qbox}[2]{%
  breakable,
  enhanced,
  colback=white,
  colframe=red,
  boxrule=0.8pt,
  arc=2mm,
  left=4mm,
  right=4mm,
  top=3mm,
  bottom=3mm,
  before upper={
    \textbf{#1}
    \medskip
  },
  after upper={
    \par\smallskip
    \hfill{\small 質問者：#2}
  }
}

\newtcolorbox{qboxok}[3]{%
  breakable,
  enhanced,
  colback=white,
  colframe=black,
  boxrule=0.8pt,
  arc=2mm,
  left=4mm,
  right=4mm,
  top=3mm,
  bottom=3mm,
  before upper={
    \textbf{#1}
    \medskip
  },
  after upper={
    \par\smallskip
    \hfill{\small 質問者：#2}
  },
  after lower={
    \par\smallskip
    \hfill{\small 回答者：#3}
  }
}

\newcommand{\pf}{\mathrm{pf\,}}

\begin{document}
% 以下のように書いてください

%--解決していない質問--
% \begin{qbox}
%   {教科書該当箇所}
%   {質問者}
  
%   質問文…
% \end{qbox}

%--解決している質問--
% \begin{qboxok}
%   {教科書該当箇所またはタイトル}
%   {質問者}
%   {回答者}
  
%   質問文…

%   \qsep

%   回答文…
% \end{qboxok}

\begin{flushleft}
  \Huge \textbf{解析力学ゼミ 質問箱 }
\end{flushleft}
\vspace{1\baselineskip}

\begin{qboxok}
  {p29  \( t \)に陽に依存しないラグランジアン}
  {蟹江}
  {蟹江}
  
  「ラグランジアンが\( t \)に陽に依存しない」ってどういう状態かよくわからない.

  \qsep

  \( q , \dot{q}\)が定まったときに,ラグランジアンが時間によらず一定の値を取るということ.
  \( q, \dot{q} \) は測定できるから,二次関数のような具体的な関数形が与えられていなくても
  陽に依存していることを定義できる.

\end{qboxok}

\begin{qboxok}
  {最速降下曲線の変形}
  {昌詩（ゼミ中）}
  {蟹江}

  \( p=\tan θ/2 \)という変形をしているが,
  \( p= \difs yx \)なのに勝手にそんなことしていいのか. 

  \qsep

  \( x ,y\)はそれぞれ \( p \)の関数として導くことができました.
  この時点で \( p \)が \( p= \difs yx \)という表式を持っているだけで,媒介変数表示になっており,サイクロイドを表せているわけです.
  たまたま媒介変数表示のパラメーターにわかりやすい性質があっただけで,媒介変数であることに変わりはないので,
  \( p \)の動く範囲がどうであっても全単射な関係を与えられる \( p=\tan θ/2 \)という変換をしていいのです.
\end{qboxok}

\begin{qboxok}
  { \( q_i \mapsto Q_i \)は全単射か }
  {蟹江}
  {全員}

  2次元極座標において \( r=0 \) としたとき,
  任意の \( θ \) で同じ座標に移ってしまうが問題ないのかな.
  
  \qsep

  高々一つの点が不都合が生まれるだけなのだから,
  \( (r,θ) = (0,0) \) の組のみ認めることにすれば良い.

\end{qboxok}

\begin{qboxok}
  {強い意味での作用反作用の法則とポテンシャル}
  {昌詩}
  {全員}

  強い意味の作用反作用の法則が成立することと,
  ポテンシャルの関数が特殊直交行列による変換に対して不変が同値なのがどういうことかわからない.

  \qsep

  強い意味での作用反作用の法則が成立するということは,
  \( \symbfit{r}_i - \symbfit{r}_j \propto \symbfit{f}_{ij} \)
  ということである.
  一般の関数 \( g \) について
  \begin{align*}
    ∇ g(|\symbfit{r}|^2) 
    =& \diff{g(ξ^2)}{ξ}\Bigg|_{ξ=x^2+y^2+z^2} (x \symbfit{e}_x + y \symbfit{e}_y + z \symbfit{e}_z )\\
    =& \diff{g(ξ^2)}{ξ}\Bigg|_{ξ=|\symbfit{r}|^2} \symbfit{r} \\
    \propto & \symbfit{r} 
  \end{align*}
  が成立することから,
  ポテンシャルが 
  \( |\symbfit{r} _i - \symbfit{r} _j | \) の関数になるときに強い意味での作用反作用の法則が成立する.
  これは粒子の相対距離についてのみ依存するということであり,
  座標系の回転によらないことと同値である.
  以上より,
  特殊直交行列は座標を回転のみ行うため疑問であった同値が示される.
  補足として,直交行列ではなく特殊直交行列である理由について述べる.
  直交行列は単に回転操作するだけでなく,空間の反転操作（例：右手座標系 → 左手座標系）
  も含む.物理において回転不変性を議論する場合,ネーターの定理を考えることから連続的な変化を想定する.
  連続な回転では鏡像操作を実現できない不連続な操作であることから特殊直交行列を考えている.
\end{qboxok}

\begin{qboxok}
  {p53 時間の常微分のチェーンルール}
  {昌詩}
  {蟹江}
  
  \( f(x(t),t) \) （ \( x(t) \)は \( x_1(t), \ldots, x_n(t) \)の略記  ）
  の時間常微分が \( \diff*{f(x(t),t)}{t}= \dot{\symbfit{r}} \cdot  ∇ f(x(t),t)  + \diffp*{f(x(t),t)}{t}\) の形で表されるのはなぜ？

  \qsep

  \begin{align*}
    \diff*{f(x(t),t)}{t}&= \diffp{f}{x_1} \dot{x}_1 + \cdots +\diffp{f}{x_n} \dot{x}_n + \diffp{f}{t}\\
    &=\dot{\symbfit{r}} \cdot  ∇ f(x(t),t)  + \diffp*{f(x(t),t)}{t}
  \end{align*}
\end{qboxok}

\begin{qboxok}
  {p61 拘束条件の独立}
  {蟹江}
  {蟹江}
   
  拘束条件が独立であることが,
  \[[\tilde{J}(q)]_{γ i} := \diffp{C_{γ}(q)}{q_i}\]
  で与えられる \( Γ \) 行 \( N \) 列行列の階数が \( Γ \) と定義されているが,
  その気持ちがわからない.

  \qsep

  \[0= C_γ (q + ε) - C_γ(q) = \sum_{i=1}^{N} \diffp{C_γ(q)}{q_i} ε_i + 𝒪(ε^2)\]
  を
  \[
    \symbfit{ε} :=
    \begin{pmatrix}
      ε_1\\
      \vdots\\
      ε_N
    \end{pmatrix}
  \]
  用いて書き換えると
  \[0= C_γ (q + ε) - C_γ(q) =  ∇_q C_γ(q) \cdot \symbfit{ε} + 𝒪(ε^2) \]
  すなわち \( ∇_q C_γ(q) \cdot \symbfit{ε} =0 \) を得る.
  \( ∇_q C_γ(q) \) と \( \symbfit{ε} \) が直交するため
  \( ∇_q C_γ(q) \) の方向には \( q \) は動かせない.
  よって 
  \( ∇_q C_γ(q) \) が \( γ =1, \ldots, Γ \) で線形独立になることは
  \( q \)が \( Γ \)  個の方向へ動けなることを示す.
  したがって \( \mathrm{rank} \tilde{J}(q) = Γ\)であるときの 自由度は 
  \( N - Γ \) となる.
\end{qboxok}

\begin{qboxok}
  {p64 (3.19)}
  {全員}
  {太駆}

  ここの変形なにしているのかわからない

  \qsep

  \begin{align*}
    &\sum_{i=1}^{N} \underbrace{\diffp{C_γ(q)}{q_i}}_{=[J(q)]_{γl}}  ε_i =0\\
    \implies & \sum_{l=N'+1}^{N} [J(q)]_{γl} ε_l +\sum_{k=1}^{N'} \diffp{C_γ(q)}{q_k} ε_k =0\\
    \implies &  \underbrace{\sum_{l=N'+1}^{N}[J(q)]_{γl} ε_l }_{=(J \symbfit{ε})_γ} = -  \sum_{k=1}^{N'} \diffp{C_γ(q)}{q_k} ε_k\\
    \implies & (J^{-1} J \symbfit{ε} )_l = ε_l = - \left( \sum_{γ=1}^{Γ}[J^{-1}]_{lγ}  \right) \left(  \sum_{k=1}^{N'} \diffp{C_γ(q)}{q_k} ε_k \right)\\
    \implies & ε_l = - \sum_{γ=1}^{Γ}  \sum_{k=1}^{N'} [J^{-1}]_{lγ} \diffp{C_γ(q)}{q_k} ε_k
  \end{align*}

\end{qboxok}

\begin{qboxok}
  {p63-p64 ラグランジュの未定乗数法の導出1}
  {全員}
  {昌詩,蟹江}

  結局この導出で何を証明しているのかよくわからない.

  \qsep

  拘束条件は式(3.7)で自動的に満たされるから \( \tilde{S}(q,λ) \)の独立な
  \( N + Γ \)  個の変数のうち \( Γ \) 個が消去できる.
  導出でしていることは,ラグランジュの未定乗数法は残りの \( N \) 個の独立変数のうち
  \( Γ \)  個を消去して 
  \( N' \)個（自由度の数）の停留値条件を解くことに帰結できているということである.

  具体的に証明の気持ちを説明する.
  式(3.19)で \( ε_l \) の \( ε_k \) による表現がわかったことで,
  一般に非０であることが判明する.
  そのため, \( i= N'+1,N'+2, \ldots,  N\)でも式(3.6)を満たす必要がある.
  もちろん \( ε_k \)が一般に非０であるため,式(3.6)はすべての\( i \)で満たされなければいけない.
  式(3.6)での未知変数の数は \( N \)   個で式(3.21)で 計 \( Γ \)個の \( λ_γ \) が消去できることがわかるので,
  式(3.22)で自由度の数だけの停留値条件に帰結することができている.

\end{qboxok}

\begin{qboxok}
  {p82 式(4.2)}
  {蟹江}
  {太駆,昌詩}

  \( q_i,Q_i \) のどちらも停留を満たす関数なら,
  軌道が複数存在してしまわないか？

  \qsep

  どちらの関数も同じ位置ベクトルを示すから,
  表現が異なるだけで軌道は変わらない.
  
\end{qboxok}

\begin{qboxok}
  {p93 \( f(q,t) = ε Λ (q,t) + 𝒪(ε^2) \) について}
  {昌詩}
  {蟹江}

  \( f \)の展開だけ \( ε \)  の1次から始まっているのはなぜ？
  そもそもどういう展開？

  \qsep

  \( f \) というのはすべての変換に対して同じ関数ではなく,変換に対するパラメータの関数になっている.
  パラメータに依存しているときそのパラメータに関するテイラー展開を実行することができる.
  \( q \) で作用が停留でなくとも
  \[f(q,t)=L(Q, \dot{Q},t) - L(q, \dot{q},t) = ε \sum_{i=1}^{N} \left( F_i(q,t) \diffp{L}{q_i} + \diff{F_i(q,t)}{t} \diff{L}{\dot{q}_i} \right) + 𝒪(ε^2) \]
  が成立する.両辺に
  \( 1/ε \) を掛けて \( ε \to 0 \) を考える.
  \begin{align*}
    \frac{f(q,t)}{ε} =& \sum_{i=1}^{N} \left( F_i(q,t) \diffp{L}{q_i} + \diff{F_i(q,t)}{t} \diff{L}{\dot{q}_i} \right) + 𝒪(ε)\\
              \xrightarrow{ε \to 0} & \sum_{i=1}^{N} \left( F_i(q,t) \diffp{L}{q_i} + \diff{F_i(q,t)}{t} \diff{L}{\dot{q}_i} \right)
  \end{align*}
  右辺が収束しているため,左辺が発散しないために \( f(q,t) \) は \( ε \)の
  0次の項が存在しない.

\end{qboxok}

\begin{qboxok}
  {p95 対称性の独立}
  {蟹江}
  {蟹江}

  対称性ってそれぞれが独立だと思っていたけどそうじゃないんじゃね？

  \qsep

  依存関係にある対称性が存在します.
  例えばガリレイ対称性が成立するならば,空間並進対称性が成立します.
  p99のガリレイ対称性に対応しているネーター保存量
  \footnote{
            章末問題の一定の外力が加わっている場合についても,
            ガリレイ対称性のネーター保存量の時間微分を行うことにより,
            並進対称性並進対称性が要請されていることがわかる.
            }
  \( \sum_{i=1}^{M}( m_i \symbfit{r} _i - t \symbfit{P} ) \) の時間微分はゼロになるはずで,
  \begin{align*}
    \diff**t{\sum_{i=1}^{M} m_i \symbfit{r} _i - t \symbfit{P} }
    =&
    \underbrace{\sum_{i=1}^{M} m_i \dot{\symbfit{r}} _i}_{= \symbfit{P} }   - \symbfit{P} - t \dot{\symbfit{P}} \\
    =&
    -t \dot{\symbfit{P}}=0
  \end{align*}

  という式変形より,運動量保存が成立する.
  連続対称性とネーター保存量は同値の関係にあるため,
  空間並進対称性があるといえる.
\end{qboxok}

\begin{qboxok}
  {p188 一行目 }
  {蟹江}
  {蟹江}

  \( \dot{Q}_i (t_f) - \dot{q}_i (t_f) = 𝒪(ε) \)って何から言えるんですか.

  \qsep

  \( Q_i (t) =  ε F_i q_i (t) +𝒪(ε) \) より自明.

\end{qboxok}

\begin{qboxok}
  {p108 式(4.86)}
  {蟹江}
  {蟹江}

  二式目で \( L(q+δq, \dot{q} + δ \dot{q} ,t) \)が \( L(q, \dot{q} ,t) \)  
  になっていること,そして最後\( \int_{t_f}^{t_f + ε} \dl{t} L(q, \dot{q} ,t)  = L(q, \dot{q},t) \big|_{t=t_f}  \) 
  に変わったこと,意味がわからん.

  \qsep

  \begin{align*}
    \int_{t_f}^{t_f + ε} \dl{t}  L(q+δq, \dot{q} + δ \dot{q} ,t) 
    =& \int_{t_f}^{t_f + ε} \dl{t} 
       \left[  
        L(q, \dot{q} ,t) + \sum_{i=1}^{N}
        \left( 
          δq_i \diffp{L}{q_i} + δ\dot{q}_i \diffp{L}{\dot{q}_i} 
        \right)  
       \right]\\
    =& \int_{t_f}^{t_f + ε} \dl{t}  L(q, \dot{q} ,t) 
       + \int_{t_f}^{t_f + ε} \dl{t} \, δq_i \Biggl(\sum_{i=1}^{N} \underbrace{\diffp{L}{q_i} - \diff**t{\diffp{L}{\dot{q}_i}[]}}_{\text{オイラー・ラグランジュ方程式}} \Biggr)\\
     & + δq_i(t_f + ε) \diffp{L}{\dot{q}_i}\Bigg|_{t=t_f + ε} - δq_i(t_f) \diffp{L}{\dot{q}_i}\Bigg|_{t=t_f} + 𝒪(ε^2)\\
    =& \int_{t_f}^{t_f + ε} \dl{t}  L(q, \dot{q} ,t) 
      + ε\underbrace{δ\dot{q}_i(t_f)}_{= - ε \ddot{q}_i (t_f)}  \diffp{L}{\dot{q}_i}\bigg|_{t=t_f} \\
     &+ ε\underbrace{δq_i(t_f)}_{= - ε \dot{q}_i (t_f)}  \diff**t{\diffp{L}{\dot{q}_i}[]}\bigg|_{t=t_f} + 𝒪(ε^2)\\
    =& \int_{t_f}^{t_f + ε} \dl{t}  L(q, \dot{q} ,t) +𝒪(ε^2)    
  \end{align*}
  また,\( \int_{t_f}^{t_f + ε} \dl{t} L(q, \dot{q} ,t)  = εL(q, \dot{q},t) \big|_{t=t_f} + 𝒪(ε^2)  \)
  については
  \[  \int_{a}^{a+ε} \dl{x} f'(x) = f(a+ε) - f(a) = εf'(a) + 𝒪(ε^2)\]
  という一般論より自明.
\end{qboxok}

\begin{qboxok}
  {p111 回転変換の\( \symbfit{\epsilon}  \) による一次近似}
  {蟹江,昌詩}
  {蟹江}
 
  \[ \symbfit{r}' = \symbfit{r} + \symbfit{\epsilon} \times \symbfit{r}   \]
  この変換と回転操作との関係性がよくわからない. 

  \qsep

  \begin{align*}
    \symbfit{r}' 
    =& R(\symbfit{\epsilon}) \symbfit{r} 
    = (I + \sum_{α} J^α \epsilon^α) \symbfit{r} 
      + 𝒪(\epsilon^2)\\
    =&\symbfit{r} + \sum_{α,β,γ} \symbfit{e}^α r^γ [J^α]^{βγ} \epsilon^α 
      + 𝒪(\epsilon^2)\\
    =&\symbfit{r} + \sum_{α,β,γ} \symbfit{e}^α ε^{αβγ} \epsilon^α r^γ 
      +𝒪(\epsilon^2)\\
    =& \symbfit{r} + \symbfit{\epsilon} \times \symbfit{r} +𝒪(\epsilon^2) 
  \end{align*}
  \footnote{ この質問だけ \( ε, \epsilon \)を区別して使っている.}
  なお,これを一成分について行ったものは(4.40)である.
  
\end{qboxok}

\begin{qboxok}
  {p111 回転対称性}
  {蟹江}
  {蟹江}

\( k/r \) の変化分が
\( 𝒪(ε^2) \) というような記述に詰まっている.
自分の考えでは,
\begin{align*}
  &\frac{k}{|\symbfit{r} + \symbfit{ε} \times \symbfit{r}  |} - \frac{k}{r} \\
  =&\frac{k}{r + |\symbfit{ε} \times \symbfit{r}  |} - \frac{k}{r} \quad (∵ \symbfit{r} \cdot (\symbfit{ε} \times \symbfit{r}) =0)\\
  =& - \frac{k}{r^2} |\symbfit{ε} \times \symbfit{r}  | + 𝒪(ε^2)
\end{align*}
という変形から \( |\symbfit{ε} \times \symbfit{r}  | = 𝒪(ε)\) となり, \( Λ \neq 0 \) 
が導かれて教科書の記述と矛盾する.

\qsep

先に何が間違っているかというと,
\( r' = r + | \symbfit{ε} \times \symbfit{r} | \) 
という等式は成立しない.

\[ \symbfit{r}'^2 = \symbfit{r}^2  + (\symbfit{ε} \times \symbfit{r} )^2=\symbfit{r}^2 + 𝒪(ε^2) \] 
より
\begin{align}
  r' =& \sqrt{\symbfit{r}^2 + 𝒪(ε^2)}  = r \sqrt{1+𝒪(ε^2)} \notag\\
  =& r + 𝒪(ε^2) 
  \label{kaiten}
\end{align}
が成立する.
あとはテイラー展開すれば,
\[\frac{k}{|\symbfit{r} + \symbfit{ε} \times \symbfit{r}  |} - \frac{k}{r} = 𝒪(ε^2)\]


\end{qboxok}

\begin{hako}
  {補足：式\eqref{kaiten}について}
ここの変形は, \( (1+x)^n \) のテイラー展開によって保証されるが,
高校範囲の極限でも納得できるので計算を残しておく .
\begin{align*}
  \frac{\sqrt{1+ε^2}-1}{ε^2} 
  =& \frac{1+ε^2 -1}{ε^2(\sqrt{1+ε^2}+1)}\\
  =&\frac{1}{\sqrt{1+ε^2} +1}
  \xrightarrow{ε \to 0}  \frac{1}{2}   
\end{align*}

\end{hako}

\newpage
\begin{qboxok}
  {ハミルトンの主関数の時間偏微分}
  {蟹江}
  {蟹江}

  ハミルトンの主関数を時間
  \( t_i,t_f \) で偏微分したときに,それぞれの時刻でのハミルトニアン
  が出てくるのはどんな意味があるんだろうか.

  \qsep

  一つの解釈として時間並進対称性のネーター保存量が,系のエネルギー（ハミルトニアン）
  に対応するからと考えることができる.
  以下で簡単のため,
  \( t \)に陽に依存しないラグランジアンについての具体的な導出をする.

  \[
    \begin{cases}
      t'= t+ε\\
      Q(t') =q(t)
    \end{cases}
  \]
  という変換を考える.
  このとき\( f(q,t) = 0 \, (Λ=0) \)となる.

  \begin{align*}
    0 
    =& S(q(t_f) , q(t_i),t_f+ε,t_i+ε) - S(q(t_f) , q(t_i),t_f,t_i)\\
    =&ε \diffp{S}{t_F} + ε \diffp{S}{t_i} + 𝒪(ε^2)\\
    =& ε\left( - \sum_{i=1}^{N} \dot{q}_i \diffp{L(q, \dot{q},t)}{\dot{q}_i} - L(q, \dot{q},t) \Bigg|_{t=t_f}  +\sum_{i=1}^{N} \dot{q}_i \diffp{L(q, \dot{q},t)}{\dot{q}_i} - L(q, \dot{q},t)\Bigg|_{t=t_i}  \right)
  \end{align*}
  よって
  \[\sum_{i=1}^{N} \dot{q}_i \diffp{L(q, \dot{q},t)}{\dot{q}_i} - L(q, \dot{q},t) \Bigg|_{t=t_i} = \sum_{i=1}^{N} \dot{q}_i \diffp{L(q, \dot{q},t)}{\dot{q}_i} - L(q, \dot{q},t) \Bigg|_{t=t_f}\]
  エネルギーが保存されている.
\end{qboxok}

\begin{qboxok}
  {p148 ハミルトニアンに偏微分を行うときのラグランジアン}
  {昌詩}
  {全員}

  \[
    \diffp{L(q, \dot{q}(q,p,t) ,t)}{t} = 
    \Biggl(
      \sum_{j=1}^{N} \underbrace{\diffp{L(q, \dot{q}, t)}{p_i}}_{=p_j} 
      \diffp{\dot{q}_j(q,p,t)}{t} 
    \Biggr)
    \Bigg|_{\dot{q}= \dot{q}(q,p,t)}  
  \]
  ここがよくわからない

  \qsep

  \( t \) の偏微分となっているが,
  \( L \) の引数である \( \dot{q} \) はハミルトニアンの形式では従属変数として扱われる.
  したがって
  合成関数の微分を \( \dot{q}(q,p,t) \) という引数について行う必要がある.

\end{qboxok}



\begin{qboxok}
  {p166 \( \mathcal{F}_1 \)での恒等変換の不能性 }
  {蟹江}
  {昌詩}

  恒等変換したら \( (p,P) \) も次元が\( N \)になるのだから,
  \( \mathcal{F}_2 \) で恒等変換が実現できることに違和感がある.

  \qsep

  \( \mathcal{F}_2(q,P,t) \) の引数の次元は恒等変換だろうが, \( 2N+1 \) 次元となる.
  したがって母関数の次元が下がらないので恒等変換を実現できる.

\end{qboxok}

\begin{qboxok}
  {p170 母関数 \( \mathcal{F}_2 \)を用いた相空間内の回転}
  {全員}
  {昌詩}

  ルジャンドル変換すると符号が逆になって出てきてしまう.

  \qsep

  ルジャンドル変換を行うと教科書の記述と符号が逆になるのは間違っていない.
  ただし,回転方向については慣習的に反時計周りを正として扱うから.
  符号を逆転させている.操作としては同じく回転であるから全く問題のない返還となっている.

\end{qboxok}

\begin{qboxok}
  {パフィアン}
  {昌詩}
  {蟹江}

  パフィアンって何？無視していい奴？

  \qsep

  実数の偶数次元交代行列（ \( A^{T} = A  \) ）から実数への写像であって,
  行列式の平方根に当たる量です.以下で前提として用いる関係式は, \( (\pf A )^2 = \det A \) となります.
  \( Ω \)が交代行列であるため, \( J Ω J^T \)  も交代行列です.
  実際,
  \begin{align*}
    (J Ω J^T)^T 
    =& (J^T)^T Ω^T J^T\\
    =& -J Ω J^T
  \end{align*}
  と定義を満たします.
  また,
  \begin{align*}
    &\left( \pf (J Ω J^T) \right)^2 = (\det J)^2 (\pf Ω)^2 \\
    \implies & |\pf (J Ω J^T)|= |\det J \, \pf Ω|
  \end{align*}
  よって
  \[f(B):= \frac{\pf (J Ω J^T)}{\det J \, \pf Ω} \]
  を定義すると, \( f \) は \( 1 \) または \( -1 \) のみを取る.
  例えば,
  \[f(I) = \frac{\pf Ω}{\pf Ω} = 1\] 
  のようになる.\( J \)の成分を連続なパラメータ\( t \)で変換することを考えるとき,
  定義より,\( f(B(t)) \)は 
  \( B \)の成分の多項式,すなわち \( t \)の連続関数となる.
  \( f \) は  \( t \)の連続関数であるが, 値が  \( 1 \) または \( -1 \) しか取れないため,
  \( -1 \) を取ることはできない.
  以上より,
  \[\pf (J Ω J^T)= \det J \, \pf Ω\]
  これを用いるとリウヴィルの定理は簡単に示せる.
  
\end{qboxok}

\begin{qboxok}
  {p186 パフィアンの定義式}
  {昌詩}
  {蟹江}

  パフィアンの定義式みると \( 1,-1 \) しか取れなくない？

  \qsep

  \( A \) の成分の積の和であって適切な\( A \)を持ってくれば任意の値を取る.
  \( \mathrm{sgn}(\sigma)  \)は符号関数.
  置換群は偶置換と奇置換でそれぞれ部分群をなすから.それぞれ \( 1, -1 \)   の値を出すもの.
  記号が異なるだけで,レヴィ・チヴィタの記号と同じ.

\end{qboxok}

\begin{qboxok}
  {p188 (7.101)}
  {蟹江}
  {蟹江}

  ここの十分性は,さも自明かのように流されたけど全然そんなことないよね？

  \qsep

  簡単です.
  式(7.42)に拡張された正準変換のポアソン括弧を代入することで,以下の関係が得られます.
  \[\{\tilde{f} , \tilde{g}\}_{(Q,P)} = \frac{1}{\rho}  \{f,g\}_{(q,p)}   \]
  これを用いると,
  任意の \( \tilde{f}, \tilde{g} \)に対して,
  \begin{align*}
    \diff{}{t} \{\tilde{f}, \tilde{g}\}_{Q,P} 
    =& \frac{1}{\rho} \diff{}{t} \{f,g\}_{q,p}\\
    =& \frac{1}{\rho} \left[ \{ \diff{f}{t} , g \}_{q,p} + \{ f , \diff{g}{t} \}_{q,p}   \right] \\
    =& \{ \diff{\tilde{f}}{t} , \tilde{g} \}_{Q,P}  + \{ \tilde{f}, \diff{\tilde{g}}{t}  \}_{Q,P} 
  \end{align*} 

  よって \( (Q,P) \) はハミルトン力学系,すなわち正準方程式が共変である.

\end{qboxok}

\begin{qboxok}
  {p189 必要性について}
  {蟹江}
  {蟹江}

  どこで正準方程式が共変という仮定を用いているのか明示して.

  \qsep

  \[ \diff{A_{ij}}{t} = \{ \dot{q}_i , p_j\}_{Q,P} + \{ q_i , \dot{p}_j\}_{Q,P}  \]
  ここです.
  正準方程式が共変であるため関数の時間微分が \( (Q,P) \)に関する ポアソン括弧のそれぞれの成分を時間微分した和と記せています.

\end{qboxok}

\begin{qboxok}
  {p189 式(7.99)と等価な式}
  {昌詩}
  {蟹江}

  式(7.102)は本当に等価なの？

  \qsep

  \( (q,p) \to (Q,P) \) への変換でのヤコビ行列を \( J \) 
  として,逆変換でのヤコビ行列を \( J' \) とすると,互いに逆行列の関係にある.

  式(7.47)より,
  \begin{align*}
    J Ω J^T 
    =&
    \begin{pmatrix}
      \{Q,Q\}_{q,p} & \{Q,P\}_{q,p}\\
      \{P,Q\}_{q,p} & \{P,P\}_{q,p}   
    \end{pmatrix}\\
    =&\rho Ω
  \end{align*}
  この等式から,
  \begin{align*}
    & J Ω J^T = \rho Ω\\
    \implies & J^{-1} J Ω J^T (J^T)^{-1} = \rho   J^{-1}  Ω  (J^T)^{-1}\\
    \implies & J' Ω J'^T = \frac{1}{\rho} Ω\\
    \implies & \text{式}(7.102) 
  \end{align*} 

\end{qboxok}

\begin{qboxok}
  {p190 \( a_k,b_k \) の取り方.}
  {蟹江}
  {蟹江}

  どう\( a_k,b_k \)を選んだら式(7.109)になるんだ.

  \qsep

  \( N \)個の和のうちある \( k \) のみ
 \( a_k = b_k =1 \) とし,それ以外を 
 \( 0 \) として考えると導かれる.

\end{qboxok}

\begin{qboxok}
  {p190 \( \sum_{l=1}^{N} M_{il} A_{lj} = \sum_{k=1}^{N} A_{ik} M_{kj}  \) }
  {蟹江}
  {蟹江}

  \[ \{\sum_{l} M_{il} q_l , p_j  \}_{Q,P} = \{q_i , \sum_{k} M_{kj} p_k  \}_{Q,P}   \]
  から
  \[ \sum_{l=1}^{N} M_{il} A_{lj} = \sum_{k=1}^{N} A_{ik} M_{kj} \]
  へは自明ではない行間を埋めてくれ.

  \qsep

  特定の \( k,l \) のみ \( M_{lj} = M_{kj} = 1   \) ととり
  その他を0とすると,任意の \( k,l \) で
  \[M_{il} A_{lj} = A_{ik} M_{kj}\]
  がわかる.
  あとは足し上げれば良い.

\end{qboxok}

\begin{hako}
  {問題1のハミルトニアン \( K \) の構成の仕方 }

  実は簡単に
  \[K(Q,P,t) = - H(q,p,t)|_{q=q(Q,P,t) , p = p(Q,P,t)} \]
  であることがこれまでの議論で理解できる.
  式(7.62),(7.63)をこの問題に当てはめると,
  \begin{align*}
    \dot{Q}_i 
    =& \{Q_i, H\}_{q,p} + \underbrace{\diffp{Q_i}{t}}_{=0}\\
    =& - \{Q_i, \tilde{H}\}_{Q,P} \quad (∵ \{Q_i,P_j\}_{q,p} = - δ_{ij}  )\\
    =& \{Q_i,-\tilde{H}\}_{Q,P}   
  \end{align*}
 ポアソン括弧を用いた正準方程式は対称的なので,
 同様の議論が \( P_i \) でもそのまま成立 .
 よって \( K(Q,P,t) = - H(q,p,t)|_{q=q(Q,P,t) , p = p(Q,P,t)}  \) 

\end{hako}

\begin{hako}
  {問題2(1)の蟹江の解き方}
  \[f_i := λ^{(-1)^{i+1} } \]
  とすると, \( P_i = f_i p_i  , \quad  Q_i = f_i q_i\)とできる.  
  このとき,
  \begin{align*}
    \{Q_i,P_j\}_{q,p} =& f_i f_j \{q_i,q_j\}_{q,p} = f_i f_j δ_{ij}  \\ 
    \{Q_i,Q_j\}_{q,p} =& f_i f_j \{q_i,q_j\}_{q,p} =0\\
    \{P_i,P_j\}_{q,p} =& f_i f_j \{p_i,p_j\}_{q,p} =0  
  \end{align*}

  また, 
  \[
    f_i f_j
    =
    \begin{cases}
      1 \quad i \neq j\\
      λ^{-2} \quad i=j=2\\
      λ^{2} \quad i=j=1 
    \end{cases}
  \]

  よって正準変換になるとき, \( λ^{2} = λ^{-2} =1  \) すなわち \( λ= \pm 1 \) 

\end{hako}






\end{document}